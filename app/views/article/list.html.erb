<h1>Article#list</h1>

<table>
    <% @articles.each do |article| %>
    <tr>
        <td><%= article.user.name %></td>
        <td><%= article.title %></td>
        <td><%= article.body %></td>
    </tr>
    <% end %>
</table>
